server:
  port: 8083

spring:
  application:
    name: order-service
  config:
    import: "configserver:http://${CONFIG_SERVER_URL:localhost:8888}"

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics